#include <stdio.h>
#include<string.h>
int main() {
    char a[] ="emb34edded12";
     int length = strlen(a);
    for(int i=0;a[i]!='\0';i++){
        if((a[i]>=48)&&(a[i]<=57))
            printf("%c ",a[i]);
       }
    return 0;
}
