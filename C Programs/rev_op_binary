#include<stdio.h>

int main()
{
    int num, rev = 0, i;

    printf("Enter a number: ");
    scanf("%d",&num);

    for(i=0;i<8;i++)
    {
        rev = rev << 1;
        rev = rev | (num & 1);
        num = num >> 1;
    }

    printf("Reversed binary value = %d",rev);

    return 0;
}
